name: Do things every 5 minutes
on:
  schedule:
    - cron: '* * * * *'
jobs:
  stale:
    runs-on: ubuntu-latest
    steps:
      - name: Execute remote SSH commands using password
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.KEY }}
          port: ${{ secrets.PORT }}
          script: |
            mongodump --db testdb --collection testCollection --out - | gzip > /backups/dump_github_action_$(date "%Y-%m-%d_%H_%M").gz
            rclone copy /backups/dump_github_action_$(date "%Y-%m-%d_%H_%M").gz r2:automated_backups